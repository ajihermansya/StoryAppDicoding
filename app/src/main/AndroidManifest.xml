<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />

            <category android:name="android.intent.category.BROWSABLE" />

            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_white"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_white"
        android:supportsRtl="true"
        android:theme="@style/Theme.StoryAppDicoding"
        tools:targetApi="31">
        <activity
            android:name=".activity_user.activity_home.UploadStoryActivity"
            android:exported="false" />
        <activity
            android:name=".activity_user.activity_home.MainMenuActivity"
            android:exported="false" />
        <activity
            android:name=".activity_user.activity_home.DetailStoryActivity"
            android:exported="false"
            android:label="@string/title_activity_detail_story"
            android:theme="@style/Theme.StoryAppDicoding" />
        <activity
            android:name=".activity_user.activity_intro.IntroActivity"
            android:exported="false" />
        <activity
            android:name=".activity_user.activity_intro.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".activity_user.activity_intro.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".activity_user.activity_home.SettingActivity"
            android:exported="false" />
        <activity
            android:name=".activity_user.activity_intro.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity_user.activity_home.MainActivity"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.rumahproduksi.storyappdicoding"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>